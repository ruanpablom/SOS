#Para executar corretamente o SOS, você terá que instalar o gnuplot e algumas libs necessárias para o gnuplot.
	
	Install:
		Para Debian e derivados incluindo Ubuntu:
			sudo apt-get install libwxgtk2.8-dev libpango1.0-dev libx11-dev libxt-dev texinfo libreadline5-d

	gnuplot:
		#Para instalar o gnuplot, descompacte 'gnuplot-4.6.6.tar.gz' e leia o arquivo INSTALL.gnu

	Compilando o algorítmo:
		#Para compilar o algorítmo, navegue até o diretório onde foi descompactado o pacote e digite 'make' para compilar
	
	Executando o algorítmo:
		#Para executar o algorítmo você deve alterar os parâmetros do arquivo 'input.in' e executar o comando './alg input.in'
